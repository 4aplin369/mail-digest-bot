name: mail-digest-bot
tariffId: "1"

meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: python3 -u mail_digest_bot.py
  persistenceMount: /data
